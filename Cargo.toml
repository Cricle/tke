[package]
name = "tke"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31.3", features = ["process", "term", "fs", "signal"] }
